import React from "react"
import Layout from "../components/layout"

//
// Goal: show list of posts on blog page
// 1. Query the title and date for each post in a blog component
//  2. Render and OL on the page
// 3. Render a li with a nested h2 (title) and p (date) for each post  - "render array of objects"
// 4. test your work

const BlogPage = () => {
  return (
    <Layout>
      <h1>Blog</h1>
      <p>Posts will show up here later on</p>
    </Layout>
  )
}

export default BlogPage
